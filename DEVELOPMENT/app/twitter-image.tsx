// Twitter card image. Reuses the same generator as the Open Graph image so
// og:image and twitter:image resolve to the same branded image.

export { default, alt, size, contentType } from './opengraph-image';
